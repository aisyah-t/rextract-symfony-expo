#!/bin/sh

# This script executes Symfony's bin/console commands using Docker Compose.
# It passes all script arguments directly to the console command.

set -e # Exit script on any error

echo "--- Running Symfony command: $@ ---"

docker compose run --rm \
  --user "$(id -u):$(id -g)" \
  app php bin/console "$@" 